class 생성

public(or default) class 클래스명 {
    // 필드부( 속성)
   private String id;
   private String pwd;

   // 생성자부
   public 클래스명 (매개변수) {
    필드 초기화
   }

   // 메소드부(기능)
   public 반환형 메소드명 (매개변수) { // 추상화 과정

   }
}


* 추상화 과정 (클래스 설계)
* 캡슐화 - 속성에 직접 접근 X / 기능으로 간접 접근 O (필드에 private 적어준다)
        - 유지보수성

static(정적 메모리 할당) : 프로그램 실행시 가장먼저 올라가고 프로그램 종료시 사라짐 (heep영역)
                        중간에 값 변경 가능
final : 값 변경 불가
       초기화를 꼭 해줘야 함